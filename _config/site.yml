name: Auto Deploy
title: Publish a website that is built using Awestruct to GitHub Pages
org: Organization
base_url: ''
profiles:
  production:
    base_url: http://atelierbram.github.io/auto-deploy
    deploy:
      host: github_pages
      # use gh-branch for repositories other than <username>.github.io
      branch: gh-pages
      # use master branch for <username>.github.io repository
      #branch: master
