name: Auto Deploy
title: Baked with Awestruct
org: Organization
base_url: 'auto-deploy'
profiles:
  production:
    base_url: http://atelierbram.github.io/auto-deploy
    deploy:
      host: github_pages
      # use gh-branch for repositories other than <username>.github.io
      branch: gh-pages
      # use master branch for <username>.github.io repository
      #branch: master
